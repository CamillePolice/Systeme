void stereoToMono(char buff[], int *length);
void switchVolume(char buff[], int *length, float volume);
void fasterThanTheOlder(char buff[], int *length);
void slowerThanTheOlder(char buff[], int *length, char *added, int *nLen);
